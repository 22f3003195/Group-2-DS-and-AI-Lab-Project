# Backend Space (rajsri2609/medical-report-ai) - Gradio + ZeroGPU.
#
# Versions are capped, not floated. Installing `gradio` unpinned today resolves
# to 6.x, where gr.Chatbot(type="messages") raises TypeError and the Chatbot
# payload schema changed - a Space rebuild would then change behaviour with no
# code change. The <6 cap is the version family this app was verified against.

# --- serving ---
gradio>=5.0,<6.0          # ZeroGPU requires Gradio 4+; <6 is what this app was verified on
gradio_client>=1.3,<2.0
spaces>=0.30              # provides @spaces.GPU; preinstalled on ZeroGPU, needed elsewhere

# --- models ---
torch>=2.8,<3.0           # ZeroGPU supports 2.8.0+ ONLY - older wheels fail to start
transformers>=4.40,<5.0
peft>=0.11,<1.0
bitsandbytes>=0.43,<1.0
accelerate>=0.30,<2.0
huggingface_hub>=0.23,<1.0

# --- OCR / imaging ---
paddleocr>=2.7,<4.0
paddlepaddle>=2.6,<4.0
pdf2image>=1.17,<2.0
opencv-python-headless>=4.9,<5.0
pillow>=10.0,<12.0
pytesseract>=0.3.10,<1.0

# --- reference-range lookup (deterministic status, no GPU) ---
openpyxl>=3.1,<4.0          # reads the workbook when rebuilding reference_db.json
rapidfuzz>=3.0,<4.0         # tier-3 fuzzy test-name resolution

# --- readability loop ---
textstat>=0.7,<1.0          # Flesch-Kincaid grade for the revise-to-grade-8 loop

# --- misc ---
numpy>=1.24,<2.0
pandas>=2.0,<3.0
